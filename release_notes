Jaakon Git v1.0.3.2

Improvements
- Amended commit info with "Merged in" item. Clicking it takes you to the commit
  where the selected commit was merged to main branch.
- In search, suggested unreachable commits are now colored red as in commit log. 


Bug fixes
- Crash when searching with an ambiguos commit hash
- Resetting files with merge conflicts did not work with all reset buttons
- Crash when resetting staged files that have unstaged changes
- Occasional crash after push
- In rebase-todo editor, fixup context menu item had wrong command mapping
