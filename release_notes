Jaakon Git v1.0.1.6

Improvements to auto-updater
 - Less intrusive update notification
 - Review release notes before and after update

Bug fixes
 - Some buttons would become permanently disabled if the operation was cancelled
 - Reverting and cherry-picking merge commits did not work